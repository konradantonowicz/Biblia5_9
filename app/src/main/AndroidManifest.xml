<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.biblia59">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".AktywnoscPokazStronga"></activity>
        <activity android:name=".AktywnoscPokazPrzypisy" />
        <activity android:name=".AktywnoscPokazKomentarz" />
        <activity android:name=".AktywnoscSzukanieSlow" />
        <activity android:name=".AktywnoscUstawienia" />
        <activity android:name=".AktywnoscPokazWersety" />
        <activity android:name=".AktywnoscGlowna">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>